# Trace 分析：a058f011097d9c3c00975d53bcefcd6a

## 范围
- 用户消息接收时间：2026-05-07 20:00:55 +0800
- trace 查询窗口：2026-05-07 20:00:00 +0800 到 2026-05-07 20:06:00 +0800
- 数据域：Tracing（`T`）+ 本地 gateway 日志

## 已确认事实
- Feishu 消息在 `20:00:55.968 +0800` 已进入网关。
- 网关在 `20:00:56.002 +0800` 已开始 `dispatching to agent (session=agent:main:main)`。
- `20:01:23` 和 `20:01:53` 的 liveness warning 都显示：
  - `active=1`
  - `queued=1`
  - `work=[active=agent:main:main(processing,...) queued=agent:main:main(processing,...)]`
- `agent/embedded` 的启动阶段直到 `20:03:01.916 +0800` 才出现，说明真正进入本轮执行是在这之后。
- trace `a058f011097d9c3c00975d53bcefcd6a` 的 root/run 起点是 `1778155382515`，即 `2026-05-07 20:03:02.515 +0800`。

## 结论
- 这条消息不是 `20:03` 才发送，而是 `20:00:55` 已经进入系统。
- `20:00:56` 到 `20:03:02` 之间存在明显的排队/占用等待。
- 当前 trace 没有把这段等待建模进去，导致 `openclaw_request/channel_ingress/agent_run` 都从实际执行开始时刻起跳，看起来像 trace 晚了两分钟多。

## 当前 trace 结构
- `openclaw_request`
- `channel_ingress`
- `agent_run`
- `session_processing`
- `runtime_orchestration`
- `model_request` x6
- `skill:dashboard`
- `skill_call:dashboard` x5
- `tool:read`
- `tool:write`
- `tool:edit`
- `tool:exec` x2
- `channel_egress`

## 判断
- 从“真正开始执行之后”的主链路看，当前结构基本正确：
  - `model -> tool -> model -> tool -> ... -> final model -> channel_egress`
  - `tool` 都正确挂在 `skill_call:dashboard` 下
- 从“用户消息进入系统开始”的完整链路看，当前结构不完整：
  - 缺少 `queue_wait` / `dispatch_wait` 一类 span
  - `channel_ingress` 时间也不是真实接入时间，而是被钳到了执行开始时间

## 建议
1. `openclaw_request` 和 `channel_ingress` 应该使用真实入站时间（如 `message.queued` / channel receive time）。
2. 增加一个显式的等待类 span，例如 `dispatch_queue` 或 `session_queue_wait`，覆盖 `20:00:56` 到 `20:03:02`。
3. `agent_run` 保持从“真正开始执行”起算，不要吞掉排队时间。
